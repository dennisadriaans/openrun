Read [`AGENTS.md`](../AGENTS.md) at the repo root. It is the single source of
truth for architecture, module boundaries, commit format, PR shape, and the hard
rules — nothing is duplicated here.

Copilot also loads root and nested `AGENTS.md` directly, so this file exists
only to guarantee the pointer on surfaces that read `.github/` first.
