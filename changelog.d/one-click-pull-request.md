- Creating a pull request from a run no longer means writing the title and body
  yourself, then remembering to commit and push first. One click now hands the
  diff to the run's own runtime and model, which groups it into Conventional
  Commits by feature; the branch is committed, pushed, and the pull request is
  opened for you. The old title/description dialog is still there as "Write the
  pull request myself".
