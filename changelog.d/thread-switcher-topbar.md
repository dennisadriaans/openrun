The conversation switcher moved out of the breadcrumb and into the run header's
action row, next to New chat. The breadcrumb now reads project / branch / run
title, and the switcher keeps its search, ⌘K, and ⌘⇧[ / ⌘⇧] shortcuts.
