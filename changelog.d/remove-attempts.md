- You no longer configure **Attempts** on an automation — racing a prompt across
  several agents, the compare screen and patch promotion are gone. Every trigger
  starts one run on the automation's own runtime, as it did before.
