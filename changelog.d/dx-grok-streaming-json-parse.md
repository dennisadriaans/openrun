You no longer watch a Grok run dump every `streaming-json` line into chat as
raw JSON — `text` deltas coalesce into assistant prose, `tool_call` /
`tool_call_update` become paired tool rows, and thought / usage / available
commands stay in the process log only.
