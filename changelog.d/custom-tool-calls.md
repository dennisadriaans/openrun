- A tool Open Run has never heard of no longer renders as a raw JSON dump.
  Custom and MCP tool calls get a readable name (`create_issue` reads as
  "Create issue"), their arguments as labelled rows instead of a blob, and a
  JSON result pretty-printed rather than run together on one line.
