- You no longer pick an access mode before a chat. The composer, the New chat
  dialog, and `/mode` are gone — every run the app starts now runs with full
  access, so nothing pauses waiting for an approval you have to answer.
