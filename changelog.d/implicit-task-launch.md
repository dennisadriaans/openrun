You no longer need `run`, `using`, or model flags to open an agent with a task
such as `create new file index-test.html with contents '123' sonnet low`.
Complete task requests select the runtime, model and effort locally, preserve
quoted task contents, and open the agent in your terminal from the command line
or Home.
