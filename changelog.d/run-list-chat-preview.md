The Runs list no longer shows only the automation name or a generic "Chat · branch"
label. Each row uses the first prompt as the chat name, with a one-line summary
underneath — the in-flight tool while it is running, or the files the agent
edited once it is done.
