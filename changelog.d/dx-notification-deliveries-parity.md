You no longer stare at a bare run id on Notifications → Deliveries — each row
links into Run History, names the notifier that fired, and the section stays
visible with an empty state when nothing has been delivered yet.
