Natural-language requests recognize model nicknames, spaced version numbers,
runtime names and reasoning-effort labels from installed model catalogs.
Model-specific effort validation keeps unsupported settings out of launches,
and quoted task text stays intact. Large catalogs retain explicitly named
models when preparing interpretation requests.
