The Codex model picker no longer lags behind your account. Open Run reads the
model list Codex itself caches, so GPT-5.6 Sol, Terra and Luna — and their Max
and Ultra reasoning levels — show up without waiting for a release here.
