You no longer click **Run now** only to get an alert when the workspace or CLI
would refuse — the button stays disabled on the Automations list and detail page,
and hover shows the same refuse reason the server would return.
