You no longer need a project, an automation, or a filled-in event list to
connect GitHub, Jira, or Linear — Install on each provider page creates an
endpoint immediately (and registers the remote webhook when you have a public
URL and credentials).
