You no longer have to discover by waiting that a Jira automation bound to a
worklog event never fires. The three worklog triggers now say they need a
Jira admin-registered hook — a connection made through Open Run Cloud cannot
receive them, because Atlassian's dynamic-webhook API refuses to subscribe.
