You no longer open Create pull request (or wonder why Push is greyed out) when
the workspace has no `origin` or `gh` is missing / not logged in — both controls
stay disabled with the refuse reason on hover (sidebar and git menu), and the
server refuses missing `origin` before calling `gh`.
