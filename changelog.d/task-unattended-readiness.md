You no longer have to reconstruct why an automation will not fire from a pile
of separate gates: the automation page lists every blocker in repair order,
including a runtime that never delivers the prompt, a webhook connection that
is gone, and a workspace that is still running or queued.
