You no longer need to leave run detail to start again. **New chat** opens an
empty composer with the same project, branch, runtime, model, effort, and access
mode, while **Repeat run** immediately sends the first prompt again.

The project picker's path display is now editable. Type or paste a folder path
and press Enter to jump straight to it instead of expanding the tree one level
at a time.
