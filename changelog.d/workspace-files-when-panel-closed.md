- You no longer lose the changed-files strip above the composer, or the diff
  overlay's file list, when the right panel is closed. Those surfaces now load
  workspace changes on every run view, not only when the panel is open.
