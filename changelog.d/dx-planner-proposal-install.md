You no longer dump planner JSON as a dead wall of text — each proposed
automation can be saved & activated (or ignored) from the Planner page and
from the planner run’s chat, using the project/workspace you already picked
when generating the plan.
