Verification checks no longer sit on top of the changed-files list on a run. They have
their own **Checks** tab next to Changed and Browse, badged with the number of failing
checks, so the Changed tab shows only what the agent actually touched.
