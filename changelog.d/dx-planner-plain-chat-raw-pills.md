You no longer see Planner (or other `--output-format plain`) Grok answers
shattered into one grey chat pill per JSON line — plain stdout stays one
assistant answer, and existing raw-only dumps fall back to the stored content.
