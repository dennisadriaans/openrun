Escape and a single Ctrl+C now clear the current input without leaving the
prompt. Escape goes back when the input is empty; pressing Ctrl+C twice still
quits. Clearing a draft also resets its preview and history position.
