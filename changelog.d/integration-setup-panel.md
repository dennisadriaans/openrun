Connecting an integration no longer drops you into an empty automation form.
The provider page now finishes the job: pick a starting point — implement
flagged tickets, start work when something moves to In Progress, triage new
tickets, or act on a comment — pick a workspace and runtime, and the trigger,
prompt and name are filled in. The Create button explains what is missing
instead of failing after the click.
