The Runs list no longer reports a Duration for chat runs. A conversation stays open between
turns, so that number counted the time you spent reading and typing as if it were runtime;
chat rows now show a dash, and Duration means what it says for scheduled, manual and webhook
runs.
