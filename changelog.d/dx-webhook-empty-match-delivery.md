You no longer mistake a verified webhook that matched zero automations for a
successful fire — Integrations → Recent deliveries shows **matched nothing**
(and links into Run History when runs did start) instead of a bare `ok`.
