You no longer arm a schedule against a missing agent CLI — Enable / save-as-Active
refuse with the same “not found on PATH” error as Run now, lists flag “CLI not on PATH”,
and Next run says it won’t fire until the binary is installed.
