Jira Connect now asks which project (or whole site) to watch, the same way
GitLab and Azure DevOps do. Creating a ticket against that project starts a
run when the automation is bound to that event — including "all events".
The default recipe still only fires on tickets labelled `agent`.
